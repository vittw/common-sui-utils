/**
 * 此处编辑函数功能的描述
 * 
 * @since 0.1.0
 * @param { number } a 第一个参数
 * @param { number } [b = 1] 第二个参数
 * @returns { number }
 * @example
 * 
 * example(1, 2);
 * // => 3
 */
export default function example(a, b = 1) {
  return a + b;
}
